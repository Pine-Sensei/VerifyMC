# Glassx 前端说明

## 性能策略（Performance Strategy）

当前项目采用“按需接入、可观测、可回收”的性能治理原则，避免出现孤岛代码：

1. **只保留已落地场景的优化代码**
   - 性能相关 composable/工具函数必须在真实页面或组件中有明确调用点。
   - 若某优化未被实际使用，不应提前保留在 `src/composables` 中。

2. **最小必要 API**
   - 已接入的性能工具仅暴露当前业务需要的方法。
   - 任何未使用的分支、参数或 fallback 必须删除，避免维护噪音。

3. **变更门禁**
   - 新增性能工具时，PR 需同时包含：
     - 使用它的页面/组件改动；
     - 验证方式（如 `type-check`/`build`）；
     - 文档更新（本文件）。

4. **定期清理**
   - 每次迭代可通过全局搜索检查 `src/composables` 是否存在零引用文件。
   - 零引用文件应优先删除，而非继续保留。

## 本次处理记录

- 已移除未被任何页面/组件引用的 `src/composables/usePerformance.ts`，以降低维护成本并避免误导后续开发。
